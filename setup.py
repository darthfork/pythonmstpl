from setuptools import setup

setup(
    name='sample_app',
    version='0.0.1', # read this from version file
    description='Flask UWSGI Docker Skeleton app',
    packages=['flask_uwsgi_docker']
)
